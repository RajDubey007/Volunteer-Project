
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script
      defer
      src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"
    ></script>


    <style>
.box{
        background: linear-gradient(112.1deg, rgb(3, 151, 232) 27.1%, rgb(47, 31, 116) 80.7%);
      }
      body{
        background: radial-gradient(circle at 74.2% 50.9%, rgb(14, 72, 222) 5.2%, rgb(3, 22, 65) 75.3%); 
      }
    </style>
  </head>
  <body class="overflow-hidden">


    <div class=" ">
 
      <div class="relative">

        

        <div
          class="absolute top-1/2 left-[5%] md:left-[10%] -translate-y-1/2 text-white"
        >
          <h1 class="text-3xl md:text-5xl font-bold" style="margin-top: 25%;">Volunteer Registration</h1>
        </div>
      </div>

      <div class="w-full h-[1200px] md:h-[60vh] lg:h-[130vh]  relative">

        <div
          class="absolute -top-[3%] md:-top-[10%] left-1/2 -translate-x-1/2 md:grid-cols-3 h-fit w-5/5 md:w-[90%] rounded shadow overflow-hidden text-white"
        >

          <div class="p-2 md:p-4 h-full box col-span-2 w-9/12 rounded-xl" style="margin-top: 15%;">
            <form method="post" action="/updateTodo/<%= recod.id %>">
              <div
                class="grid grid-cols-1 md:grid-cols-2 gap-8 py-6 px-4 md:py-12 md:px-8 text-sm"
              >
                <!-- {/* name input  */} -->
                <div class="flex flex-col gap-1">
                  <label class="font-semibold">
                    Name <span class="text-red-500">&#42;</span>
                  </label>
                  <input
                    class="border-[1px] border-white bg-black p-2 rounded-md"
                    placeholder="Enter Your Name"
                    required
                    name="name"
                    type="text"
                    value="<%= recod.name %>"
                  />
                </div>

                <!-- {/* email input  */} -->
                <div class="flex flex-col gap-1">
                  <label class="font-semibold">
                    Email <span class="text-red-500">&#42;</span>
                  </label>
                  <input
                    class="border-[1px] border-white bg-black p-2 rounded-md"
                    placeholder="Enter Your Email"
                    required
                    name="email"
                    type="email"
                    value="<%= recod.email %>"
                  />
                </div>

                <!-- {/* phone number input  */} -->
                <div class="flex flex-col gap-1">
                  <label class="font-semibold">
                    Phone <span class="text-red-500">&#42;</span>
                  </label>
                  <input
                    class="border-[1px] border-white bg-black p-2 rounded-md"
                    placeholder="Enter Your Phone Number"
                    required
                    name="phone"
                    type="tel"
                    value="<%= recod.phone %>"
                  />
                </div>

                <!-- {/* subject input  */} -->
                <div class="flex flex-col gap-1">
                  <label class="font-semibold">
                    Subject <span class="text-red-500">&#42;</span>
                  </label>
                  <input
                    class="border-[1px] border-white bg-black p-2 rounded-md"
                    placeholder="Enter Your Subject"
                    required
                    name="subject"
                    type="text"
                    value="<%= recod.subject %>"
                  />
                </div>

                <!-- {/* message input  */} -->
                <div class="flex flex-col gap-1 md:col-span-2">
                  <label class="font-semibold">
                    Message <span class="text-red-500">&#42;</span>
                  </label>
                  <input
                    class="border-[1px] border-white bg-black p-2 rounded-md"
                    placeholder="Enter Your Message"
                    required
                    name="msg"
                    type="text"
                    value="<%= recod.msg %>"
                  />
                </div>

                <button
                  type="submit"
                  class="py-2 px-4 md:py-4 md:px-6 bg-black rounded-md border-2 border-white flex items-center gap-2 hover:scale-95 transition-all"
                >
                  <span class="text-xl">Submit</span>

                </button>
              </div>
            </form>

            <!-- {/* submit button div  */} -->
            <div
              class="flex items-center justify-center md:justify-end py-4 px-8"
            >
              <!-- {/* submit button  */} -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
